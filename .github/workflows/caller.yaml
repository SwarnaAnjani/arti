name: Main Workflow

on:
  push:
    branches: ["main"]

jobs:
  call-reusable:
    uses: ./.github/workflows/original_workflow.yaml

  # call reusable from another repo
  # uses: shaik-byte/original-repo/.github/workflows/another_repo_workflow.yaml@main


